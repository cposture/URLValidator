CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(URLVALIDATOR)

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++0x -g -Wall -O3")

AUX_SOURCE_DIRECTORY(. SOURCE_FILES)

ADD_EXECUTABLE(URLValidator ${SOURCE_FILES})
